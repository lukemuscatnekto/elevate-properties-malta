# Netlify — https://docs.netlify.com/configure-builds/file-based-configuration/
[build]
  command = "npm run build"
  publish = "dist"

# SPA: serve index.html for non-file paths (e.g. /privacy direct visits)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
