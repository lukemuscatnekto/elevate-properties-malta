/**
 * Criteria applied when the user runs a search from the homepage hero.
 * Values are stable option keys (see Hero selects).
 */
export type HeroSearchCriteria = {
  location: string;
  propertyType: string;
  budget: string;
  bedrooms: string;
};
